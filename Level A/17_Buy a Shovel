// -------------------------------------------------------------
// Problem: 732A - Buy a Shovel
//
// Link: https://codeforces.com/contest/732/problem/A
// -------------------------------------------------------------

// Description:
// Polycarp wants to buy shovels, each costing k burles.
// He has unlimited 10-burle coins and exactly one coin of r burles.
// He wants to buy the minimum number of shovels such that
// the total cost can be paid without receiving any change.
// -------------------------------------------------------------

// Example:
// Input:
// 117 3
//
// Output:
// 9
// -------------------------------------------------------------

// Approach / Idea:
// - Try buying i shovels (1 ≤ i ≤ 10).
// - Compute (k * i) % 10.
// - If the last digit is 0 or equals r, payment is possible.
// - Print the smallest valid i.
// Type: Implementation / Brute Force
// -------------------------------------------------------------

// Time Complexity: O(1)   // at most 10 iterations
// Memory Complexity: O(1) // only a few variables used
// -------------------------------------------------------------

#include <bits/stdc++.h>
#define Fast ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
 
using namespace std ;
 
int main()
{
  short k , r ;
  cin>>k>>r ;
  
  short k2 = k ;
  
  for(int i = 1 ; i <= 10 ;++i)
  {
    int mod = (k*i) % 10 ;
    
    if( mod==0 || mod == r)
    {
        cout<<i<<"\n";
        break ;
    }
  }
}
