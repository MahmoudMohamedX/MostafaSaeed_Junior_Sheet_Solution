// -------------------------------------------------------------
// Problem: 709A - Juicer
//
// Link: https://codeforces.com/contest/709/problem/A
// -------------------------------------------------------------

// Description:
// Kolya wants to make orange juice using a juicer.
// He has n oranges with different sizes.
// Oranges are processed in order.
//
// Rules:
// - If the orange size is greater than b, it is thrown away.
// - Otherwise, it is squeezed and its size is added to the waste.
// - If the total waste size becomes strictly greater than d,
//   Kolya empties the waste section and continues.
//
// The task is to determine how many times the waste section
// will be emptied.
// -------------------------------------------------------------

// Example:
// Input:
// 2 7 10
// 5 6
// Output:
// 1
// -------------------------------------------------------------

// Approach / Idea:
// - Iterate through all oranges in order.
// - Ignore oranges with size greater than b.
// - Accumulate the sizes of valid oranges.
// - Whenever the accumulated size exceeds d,
//   reset it and increment the counter.
// Type: Implementation / Simulation
// -------------------------------------------------------------

// Time Complexity: O(n)   // n = number of oranges
// Memory Complexity: O(1) // constant extra space
// -------------------------------------------------------------

#include <iostream>
#include <stack>
#include <queue>
#include <algorithm>
#include <vector>
#include <string>
#include <iomanip>
#include <cmath>
#include <functional>
 
#define Fast ios::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr);
 
using namespace std;
 
 
int main()
{
    
    //  إِنَّهُۥ مَن يَتَّقِ وَيَصْبِرْ فَإِنَّ ٱللَّهَ لَا يُضِيعُ أَجْرَ ٱلْمُحْسِنِينَ﴾ - يوسف ٩٠﴿
 
    int n = 0, b = 0, d = 0;
    cin >> n >> b >> d;
 
    int times = 0, orange = 0;
    long long cnt = 0;
   
 
    while (n--)
    {
        cin >> orange;
 
        if (orange <= b)
            cnt += orange;
 
        if (cnt > d)
        {
            cnt = 0;
            times++;
        }
    }
 
    cout << times;
 
    //  إِنَّهُۥ مَن يَتَّقِ وَيَصْبِرْ فَإِنَّ ٱللَّهَ لَا يُضِيعُ أَجْرَ ٱلْمُحْسِنِينَ﴾ - يوسف ٩٠﴿
 
    return 0;
}
