/ -------------------------------------------------------------
// Problem: 405A - Gravity Flip
// Link: https://codeforces.com/contest/405/problem/A
// -------------------------------------------------------------

// Description:
// Little Chris has a toy box with n columns of cubes. Each column i contains ai cubes. 
// Initially, gravity pulls cubes downwards. When Chris switches gravity to the right, 
// all cubes slide to the right side of the box. You need to compute the number of cubes 
// in each column after the gravity switch.
// -------------------------------------------------------------

// Example:
// Input:
// 4
// 3 2 1 2
// Output:
// 1 2 2 3
// -------------------------------------------------------------

// Approach / Idea:
// Since switching gravity to the right effectively sorts the columns in non-decreasing order,
// we can just read the array of cubes and sort it. 
// The sorted array represents the heights of the columns after gravity flips.
// Type: Implementation
// -------------------------------------------------------------

// Time Complexity: O(n log n)  // due to sorting
// Memory Complexity: O(n)     // storing the array
// -------------------------------------------------------------

#include <bits/stdc++.h>
#define Fast ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
using namespace std;

int main()
{
    Fast;

    //  إِنَّهُۥ مَن يَتَّقِ وَيَصْبِرْ فَإِنَّ ٱللَّهَ لَا يُضِيعُ أَجْرَ ٱلْمُحْسِنِينَ﴾ - يوسف ٩٠﴿

    int n;
    cin >> n;

    int arr[n];
    for (int i = 0; i < n; ++i)
        cin >> arr[i];

    sort(arr, arr + n); // gravity flip -> sort columns

    for (int i = 0; i < n; ++i)
        cout << arr[i] << " ";

    cout << endl;

    //  إِنَّهُۥ مَن يَتَّقِ وَيَصْبِرْ فَإِنَّ ٱللَّهَ لَا يُضِيعُ أَجْرَ ٱلْمُحْسِنِينَ﴾ - يوسف ٩٠﴿

    return 0;
}
