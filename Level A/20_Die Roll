// -------------------------------------------------------------
// Problem: 9A - Die Roll
//
// Link: https://codeforces.com/contest/9/problem/A
// -------------------------------------------------------------

// Description:
// Yakko, Wakko, and Dot roll a standard six-sided die.
// Yakko gets Y points, Wakko gets W points.
// Dot will roll after them.
//
// Dot wins if her roll is greater than or equal to
// the maximum of Yakko's and Wakko's rolls
// (they allow her to win in case of a tie).
//
// The task is to calculate the probability that Dot wins.
// The probability should be printed as an irreducible fraction
// in the form A/B.
// -------------------------------------------------------------

// Example:
// Input:
// 4 2
// Output:
// 1/2
// -------------------------------------------------------------

// Approach / Idea:
// - Determine the maximum value between Yakko's and Wakko's rolls.
// - Count how many outcomes (from 1 to 6) allow Dot to win.
// - Express the result as a simplified fraction.
// - Since the total number of outcomes is fixed (6),
//   we can directly map each maximum value to its probability.
// Type: Implementation / Math
// -------------------------------------------------------------

// Time Complexity: O(1)   // constant time
// Memory Complexity: O(1) // constant space
// -------------------------------------------------------------

#include <iostream>
#include <stack>
#include <queue>
#include <algorithm>
#include <vector>
#include <string>
#include <iomanip>
#include <cmath>
#include <functional>
 
#define Fast ios::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr);
 
using namespace std;
 
 
int main()
{
    
    //  إِنَّهُۥ مَن يَتَّقِ وَيَصْبِرْ فَإِنَّ ٱللَّهَ لَا يُضِيعُ أَجْرَ ٱلْمُحْسِنِينَ﴾ - يوسف ٩٠﴿
 
    Fast;
 
    short x = 0, y = 0;
    cin >> x >> y;
 
    short Max = max(x, y);
 
    if (Max == 6) cout << "1/6";
    else if (Max == 5) cout << "1/3";
    else if (Max == 4) cout << "1/2";
    else if (Max == 3) cout << "2/3";
    else if (Max == 2) cout << "5/6";
    else cout << "1/1";
 
    //  إِنَّهُۥ مَن يَتَّقِ وَيَصْبِرْ فَإِنَّ ٱللَّهَ لَا يُضِيعُ أَجْرَ ٱلْمُحْسِنِينَ﴾ - يوسف ٩٠﴿
 
    return 0;
}
